# bemain's flatpak repo

This repo contains my personal flatpaks.

To subscribe to the repo, run
```bash
flatpak --user remote-add --if-not-exists bemain "https://bemain.github.io/flatpaks/index.flatpakrepo"
```